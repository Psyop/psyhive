#!/usr/bin/env python

import os
import sys

from psyhive.utils import abs_path, find

if __name__ == '__main__':
    if len(sys.argv) > 1:
        _paths = [abs_path(sys.argv[1])]
    else:
        _paths = find(depth=1)
    for _path in _paths:
        print _path